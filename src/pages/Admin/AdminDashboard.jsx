import React from "react";
import useAuthStore from "@stores/useAuthStore";

export default function AdminDashboard() {
  // const { getRoleFromToken } = useAuthStore();

  // console.log(getRoleFromToken());

  return <div>Dashboard</div>;
}
